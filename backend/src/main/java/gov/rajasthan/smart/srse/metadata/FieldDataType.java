package gov.rajasthan.smart.srse.metadata;

/**
 * Catalogue data type for future officer-UI input validation.
 * Intrinsic field metadata — not consumed by the compiler yet.
 */
public enum FieldDataType {
    NUMBER,
    STRING,
    BOOLEAN,
    DATE
}
